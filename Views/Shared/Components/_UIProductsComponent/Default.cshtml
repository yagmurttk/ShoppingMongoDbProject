@model ProductViewModel
<section class="bg0 p-t-23 p-b-140">
    <div class="container">
        <div class="p-b-10">
            <h3 class="ltext-103 cl5">
                Ürün Genel Bakışı
            </h3>
        </div>

        <div class="flex-w flex-sb-m p-b-52">
            <div class="flex-w flex-l-m filter-tope-group m-tb-10">

                <button class="stext-106 cl6 hov1 bor3 trans-04 m-r-32 m-tb-5 how-active1" data-filter="*">
                    Tüm Ürünler
                </button>

                @foreach (var item in Model.Categories)
                {
                    <button class="stext-106 cl6 hov1 bor3 trans-04 m-r-32 m-tb-5" data-filter=".@item.CategoryName.ToLower().Replace(" ", "-")">
                        @item.CategoryName
                    </button>
                }
            </div>
        </div>

        <div class="row isotope-grid">
            @foreach (var product in Model.Products)
            {
                var category = Model.Categories.FirstOrDefault(x => x.CategoryId == product.CategoryId);
                var categoryClass = category?.CategoryName?.ToLower().Replace(" ", "-") ?? "uncategorized";

                <div class="col-sm-6 col-md-4 col-lg-3 p-b-35 isotope-item @categoryClass">

                    <div class="block2">
                        <div class="block2-pic hov-img0">
                            <img src="@product.ImageUrl" alt="IMG-PRODUCT" />

                            <a href="/Products/Detail/@product.ProductId" class="block2-btn flex-c-m stext-103 cl2 size-102 bg0 bor2 hov-btn1 p-lr-15 trans-04">
                                Hızlı Görünüm
                            </a>
                        </div>

                        <div class="block2-txt flex-w flex-t p-t-14">
                            <div class="block2-txt-child1 flex-col-l ">
                                <a href="product-detail.html" class="stext-104 cl4 hov-cl1 trans-04 js-name-b2 p-b-6">
                                    @product.ProductName
                                </a>

                                <span class="stext-105 cl3">
                                    ₺@product.ProductPrice
                                </span>
                            </div>

                            <div class="block2-txt-child2 flex-r p-t-3">
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>

        <!-- Daha Fazla Yükle -->
        <div class="flex-c-m flex-w w-full p-t-45">
            <a href="#" class="flex-c-m stext-101 cl5 size-103 bg2 bor1 hov-btn1 p-lr-15 trans-04">
                Daha Fazla Yükle
            </a>
        </div>
    </div>
</section>
